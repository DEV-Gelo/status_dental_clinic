import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    save: {
      main: "#1976d2",
      contrastText: "#fff",
    },
    cancel: {
      main: "#444",
      contrastText: "#fff",
    },
    appointment: {
      main: "#006eff",
      contrastText: "#fff",
    },
  },
});
